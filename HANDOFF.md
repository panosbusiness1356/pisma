# HANDOFF — PISMA site

Κατάσταση έργου για συνέχιση σε νέο chat. Τελευταία ενημέρωση: **29/07/2026**.

> **ΠΡΩΤΗ ΚΙΝΗΣΗ ΚΑΘΕ SESSION: `git pull origin main`.** Το repo υπάρχει σε ΔΥΟ
> λογαριασμούς χρήστη στο ίδιο MacBook (Ariadni: `~/Desktop/pisma` · panospap:
> `~/Downloads/pisma`) — η χρήστρια εναλλάσσεται. Κάθε λογαριασμός έχει δικό του
> SSH κλειδί στο GitHub (panosbusiness1356: «MacBook» = Ariadni, «mac» = panospap).
> Χωρίς pull πρώτα, δουλεύεις πάνω σε παλιά έκδοση.

## ΤΙ ΕΓΙΝΕ ΣΤΟ ΤΕΛΕΥΤΑΙΟ SESSION (28/07 — προετοιμασία SEO/AEO/GEO) — διάβασε πρώτα αυτό

Στόχος: όλα όσα γίνονται ΠΡΙΝ το ξεκλείδωμα του site (το middleware.ts μένει ως έχει).

1. **Νέα ενότητα «Οδηγοί» (/odigoi/)** — 5 answer-first άρθρα για SEO/AEO/GEO:
   - `seo-me-apla-logia`, `na-se-proteinei-to-chatgpt`, `profil-google-maps`,
     `perissoteres-kritikes-google`, `poso-kostizei-ena-site` + σελίδα-ευρετήριο.
   - Νέο layout `src/layouts/Guide.astro`: ερώτηση-h1, κουτί «Η σύντομη απάντηση»
     πρώτο, πρόζα με h2-ερωτήσεις, FAQ (FAQPage schema), Article JSON-LD, CtaBand
     προς /elegxos/. Οι τιμές ΜΟΝΟ μέσω imports από pricing.ts (κανόνας τηρήθηκε).
   - Link «Οδηγοί» σε Nav + Footer. Οι κανόνες copy (όχι πλαστά νούμερα, απλά
     λόγια, έμφαση AI) τηρήθηκαν — έγινε και πέρασμα επιμέλειας.
2. **Φόρμα /elegxos/ → FormSubmit** (τέλος το τελευταίο mailto): ίδιο pattern με
   epikoinonia (fetch στο FORM_ENDPOINT, έλεγχος json.success, εφέ «Δ»: spinner →
   τικ → κομφετί → auto-reset 6s). Δοκιμάστηκε live: επιτυχής υποβολή end-to-end.
3. **sitemap.xml**: έφυγε το παρκαρισμένο `/timologisi-2026/` (έβγαζε 404),
   μπήκαν `/odigoi/` + οι 5 οδηγοί.
4. **launch.json**: προστέθηκε `pisma-site-4323` (όταν 4321/4322 πιασμένες από άλλα chats).
4β. **Αντιστάθμιση για το ανύπαρκτο (ακόμα) Google Business Profile** — το site έγινε
   η πλήρης «ταυτότητα» της PISMA για Google & AI:
   - index: ProfessionalService schema με @id, 9 περιοχές (δήμοι νοτίων), knowsAbout,
     hasOfferCatalog με ΟΛΟ τον τιμοκατάλογο από pricing.ts (18 υπηρεσίες).
   - Base: WebSite JSON-LD σε κάθε σελίδα. Guide: BreadcrumbList. /odigoi/: ItemList.
   - **/llms.txt**: generated endpoint (src/pages/llms.txt.ts) από config+pricing+guides —
     σύνοψη επιχείρησης/τιμών/οδηγών για AI crawlers, πάντα συγχρονισμένο.
   - Η λίστα οδηγών πλέον στο **src/data/guides.ts** (μία πηγή για /odigoi/ + llms.txt).
   - robots.txt: ρητά Allow για GPTBot/ClaudeBot/PerplexityBot/Google-Extended κ.ά.
4γ. **ΝΕΟΣ ΚΑΝΟΝΑΣ COPY (28/07, μνήμη no-small-business-wording):** ΠΟΤΕ
   «μικρή/μικρομεσαία επιχείρηση», «ΜμΕ», «SMB» πουθενά στο site — γράφουμε
   «επιχείρηση / η επιχείρησή σου / ιδιοκτήτες επιχειρήσεων». Καθαρίστηκαν 11 αρχεία.
4δ΄. **Ταμπλό: νέες ερωτήσεις με μεθοδολογία** — 1 ερώτηση ανά εργαλείο του pricing.ts,
   ερωτήσεις-γεγονότα (όχι αυτοβαθμολόγηση), ενιαία σκάλα 4 επιλογών
   (τίποτα 0 → σποραδικά 0.5 → με το χέρι/τακτικά 1 → αυτόματα 2), ουδέτερες
   διατυπώσεις (έφυγαν «παρατημένη», emoji). Ετυμηγορία: δείχνει και τον πιο
   αδύναμο άξονα όταν η διαφορά ≥15. Bugfix: `.dots[hidden]{display:none}`
   (το display:flex νικούσε το hidden στην οθόνη αποτελεσμάτων).
4δ. **+2 οδηγοί υπηρεσιών** (τι είναι, ΟΧΙ πώς γίνεται, απλά λόγια χωρίς εξυπνάδες):
   `ti-einai-oi-aftomatismoi` (οι 6 αυτοματισμοί με μία πρόταση ο καθένας + τιμές
   από pricing.ts) και `diaxeirisi-social-media` (επιλογές social + πίνακας τιμών).
   Σύνολο οδηγών: 7, λίστα στο src/data/guides.ts.
5. **Εκκρεμότητες χρήστριας (εκτός κώδικα):** δημιουργία Google Business Profile,
   Search Console (επαλήθευση DNS), εγγραφές σε καταλόγους με ίδια στοιχεία (NAP).
   Το ξεκλείδωμα = σβήσιμο middleware.ts + push (τότε: request indexing στο GSC).

## ΠΡΟΗΓΟΥΜΕΝΟ SESSION (27–28/07)

**Πλήρες redesign της σελίδας /apodeixeis/ («Αποτελέσματα»)** — commits cfd0c91 + 10b31b5:

1. **Νέα δομή σελίδας:** Hero «Αποτελέσματα που μετριούνται.» → «Πώς μετράμε» (3 βήματα:
   01 Πριν ξεκινήσουμε = μέτρηση στον Έλεγχο · 02 Στους 3 μήνες = «Μετράμε ξανά και
   συγκρίνουμε: πού ήσουν, πού είσαι» · 03 Αν δεν ανέβηκαν = «Σου το λέμε πρώτοι. Και δεν
   σε κρατάει κανένα συμβόλαιο») → «Τι θα δεις να αλλάζει» (6 κάρτες-μετρικές με βέλη
   ↑πράσινο/↓πορτοκαλί) → «Η επιχείρησή σου, σε άνοδο» (animated SVG: γραμμή που
   σχεδιάζεται, chips Τηλέφωνα/Κριτικές/Επισκέψεις σκάνε με αναπήδηση, κομφετί στην
   κορυφή, τελικό pill «Κι εσύ με λιγότερες ώρες στο γραφείο ✓» — IntersectionObserver,
   loop όσο ορατό, στατικό σε prefers-reduced-motion) → CTA «Ξεκινάμε με μέτρηση».
2. **ΤΙ ΑΦΑΙΡΕΘΗΚΕ (αποφάσεις χρήστριας, ΜΗΝ επανέλθουν):** τα «ενδεικτικά σενάρια» με
   επινοημένα νούμερα πριν→μετά (18→2 ώρες κ.λπ.) — κρίθηκαν αντιεπαγγελματικά· το
   placeholder «Μαρτυρίες»· το ξεχωριστό section «Ζωντανό» (έγινε μισή πρόταση στο CTA).
   Απορρίφθηκαν επίσης στη διαδρομή: κινητό με ειδοποιήσεις, διακόπτης Χωρίς/Με PISMA,
   μαγαζί που ανάβει, λίστα που τσεκάρεται — κρατήθηκε ΜΟΝΟ το ανοδικό γράφημα.
3. **Νέοι κανόνες copy (μνήμη: no-fake-numbers-simple-copy):** όχι πλαστά νούμερα/σενάρια
   πουθενά· απλά λόγια χωρίς «έξυπνες» φράσεις· **έμφαση στο AI**: όπου γράφεται Google,
   δίπλα και AI/ChatGPT όπου ταιριάζει (έγινε ήδη: κάρτα «Πελάτες από Google & AI»,
   «θέση σε Google και AI» στο βήμα 01, «Site, Google & AI…» στην ανάλυση του /elegxos/).
4. **Δεσμεύσεις που μπήκαν δημόσια στη σελίδα (πρέπει να τηρούνται):** καταγραφή «πριν»
   στον Έλεγχο, επαναμέτρηση στους 3 μήνες (στα μηνιαία κάθε μήνα), καμία δέσμευση/συμβόλαιο
   στα μηνιαία.
5. **Τεχνικό μάθημα:** τα scoped styles της σελίδας χάνουν από global κλάσεις ίδιας
   ειδικότητας (π.χ. `.icon-chip`) — λύση: διπλός selector `.icon-chip.metric-chip--up`.

## ΠΡΟΗΓΟΥΜΕΝΟ SESSION (26–27/07)

1. **Νέο project: Εργαλείο κριτικών Google** (QR στο μαγαζί → τροχός δώρων → αίτημα
   κριτικής → κουπόνι). Δύο demos ΧΩΡΙΣ backend, μέσα στο site:
   - `/demo/pitsaria/` («La Nonna», μασκότ ο σερβιτόρος **Τζίνο** — SVG, ζωγραφισμένος custom)
   - `/demo/kommotirio/` («Salon Mirella», μασκότ η κομμώτρια **Μιρέλλα**)
   - Αισθητική: λιτά κείμενα, ΧΩΡΙΣ συννεφάκια-λόγια (οι μασκότ κάνουν τυχαίες κινήσεις:
     βόλτες/σάλτα/στροφές). Οθόνη κριτικής = «εναλλακτική Β»: μεγάλη μασκότ σερβίρει
     κλειδωμένο δώρο, τίτλος «Κάνε κριτική για να ξεκλειδώσεις το δώρο σου», κουμπί με
     αυθεντικό G Google και ΜΟΝΟ «Άφησε κριτική», «Παράλειψη» μικρό κάτω κάτω.
   - Η κριτική στο demo = mock φύλλο «Προσομοίωση» in-page. Ο **soft check** (άνοιγμα
     πραγματικής Google + ξεκλείδωμα στην επιστροφή) ΥΛΟΠΟΙΗΘΗΚΕ και ΑΦΑΙΡΕΘΗΚΕ
     (δεν δούλευε χωρίς πραγματικό link) — κώδικας στο commit **d5cb7d1**, επαναφορά όταν
     υπάρξει πελάτης με writereview link (placeid). Δες memory: reviews-soft-check-parked.
2. **Email site:** `info@pisma.gr` παντού (config.ts). Το domain λαμβάνει μέσω ImprovMX·
   το info@ προωθείται σε «άλλο inbox» της χρήστριας (ΟΧΙ στο συνδεδεμένο gmail).
3. **Φόρμες = ΛΕΙΤΟΥΡΓΙΚΕΣ (τέλος το mailto):** FormSubmit, ενεργοποιημένο από τη χρήστρια.
   `FORM_ENDPOINT = 'https://formsubmit.co/ajax/info@pisma.gr'` (src/data/pricing.ts).
   ΠΡΟΣΟΧΗ: το FormSubmit απαντά HTTP 200 και σε αποτυχία — ελέγχουμε `json.success`.
   Και οι δύο φόρμες (times + epikoinonia) έχουν **εφέ επιτυχίας «Δ»**: spinner στο κουμπί →
   οθόνη με πράσινο τικ που ζωγραφίζεται → κομφετί → ήχος → auto-επαναφορά φόρμας σε 6s.
4. **Διορθώσεις bugs (μάθε από αυτά):**
   - «Στείλτε μου αυτή την προσφορά» ήθελε 2 πατήματα: το native scrollIntoView ακυρώνεται
     από το raf loop του Lenis → πλέον `window.lenis.scrollTo()` (το Base.astro εκθέτει
     `window.lenis`) + focus με delay 700ms.
   - Η οθόνη επιτυχίας «κολλούσε»: το author `#offer-form{display:grid}` υπερισχύει του
     `hidden` → προστέθηκε `#offer-form[hidden]{display:none}`.
   - Το τικ κόλλαγε αριστερά: global `svg{display:block}` → `.fd-tick{margin:0 auto}`.
   - ΤΕΛΕΥΤΑΙΟ ΓΝΩΣΤΟ ΘΕΜΑ: η χρήστρια ανέφερε ότι «δεν φτιάχτηκε» το κεντράρισμα —
     ελέγχθηκε ότι το live CSS + live μετρήσεις είναι ΣΩΣΤΑ (πιθανότατα cache της).
     Αν το ξαναπεί: ζήτα σκληρό refresh/incognito, μετά έλεγξε ξανά οπτικά.
5. **Google/SEO:** το site ΔΕΝ φαίνεται στην Google λόγω κλειδαριάς (όλα 401+noindex,
   επιβεβαιωμένο και με site:pisma.gr = 0 αποτελέσματα). Απόφαση: μένει έτσι μέχρι να
   «τελειώσει το χτίσιμο»· μετά υβριδικό (δημόσιο site, /demo/* με κωδικό) + Search Console.

## ΝΕΟΣ ΠΑΓΙΟΣ ΚΑΝΟΝΑΣ (23/07/2026): Ο τιμοκατάλογος = πηγή αλήθειας

Το site προβάλλει ΜΟΝΟ υπηρεσίες που υπάρχουν στο `src/data/pricing.ts`. Αφαιρέθηκαν
(απόφαση χρήστη, «τίποτα δεν επιστρέφει»): ηλεκτρονική τιμολόγηση/myDATA/προθεσμία 1/10
(η σελίδα πάρκαρε ως `src/pages/_timologisi-2026.astro` — επαναφορά = μετονομασία χωρίς
underscore + link στο footer), «Τιμολόγια χωρίς πληκτρολόγηση» (καταχώρηση), «Προσφορά
αυθημερόν», «Τέλος οι χαμένες προθεσμίες». Πριν γραφτεί οπουδήποτε νέα υπηρεσία,
έλεγχος ότι υπάρχει στο pricing.ts.

ΠΡΟΣΟΧΗ live checks: το pisma.gr κάνει 308 → **www.pisma.gr** — τα curl πάνε στο www
(με το cookie της κλειδαριάς), αλλιώς τα until-loops κολλάνε.

## Το έργο

- **Τι είναι:** Site της PISMA — ψηφιακή παρουσία (site/Google/AI) και αυτοματοποίηση
  διαδικασιών για ελληνικές ΜμΕ.
- **Stack:** Astro (static), καθαρό CSS/TS, χωρίς React/Tailwind. Μόνη dependency: `lenis`.
- **Repo:** `~/Desktop/pisma` → GitHub `panosbusiness1356/pisma` (branch `main`).
  ΠΡΟΣΟΧΗ: `dist/` και `node_modules/` είναι tracked στο git (μην το «διορθώσεις»).
  Το git identity ορίστηκε τοπικά στο repo (Ariadni Barlou) — αν λείψει, `git config user.name/email` τοπικά.
- **Deploy:** Vercel → **pisma.gr** (www redirect) + pisma-flame.vercel.app, auto-deploy από `main`.
- **ΚΛΕΙΔΑΡΙΑ:** Το site θέλει κωδικό **«pisma2026»** — `middleware.ts` στη ρίζα (Vercel Edge).
  Για curl-έλεγχο στο live: header `Cookie: pisma_code=pisma2026`. Αφαίρεση = σβήσε το αρχείο.
  Όσο υπάρχει, το Google ΔΕΝ βλέπει το site.
- **Dev server:** `.claude/launch.json` → `pisma-site-4322` (port 4322· το 4321 ίσως πιασμένο από άλλο chat).

## Πάγιος κανόνας εργασίας

Μετά από **κάθε** ολοκληρωμένη αλλαγή: `npx astro build` → `git add src/ dist/ (+ ό,τι άλλο)`
→ commit με σύντομο ελληνικό μήνυμα → `git push origin main`. Χωρίς να ρωτάς.
Μετά το push, επιβεβαίωση στο live με curl (με το cookie της κλειδαριάς).

## Design

- **Θέμα:** σκούρο μωβ-μαύρο (n8n-style). Παλέτα ΜΟΝΟ μέσω CSS variables στο `src/styles/global.css`.
  Δοκιμάστηκαν και απορρίφθηκαν: χρωματιστά accents ανά section, φωτεινό θέμα «Κρέμα & Πηλός»,
  χρωματικό «ταξίδι» — ΜΗΝ ξαναπροταθούν (δες memory: color-journey-restore-point).
- **Φόντο:** οι λάμψεις κορυφής ζουν σε `body::before` (στρώμα 100vh) — ΟΧΙ στο body background
  (άφηνε ορατή «γραμμή» στο όριο header/main).
- **Fonts:** Unbounded (logo), Commissioner (κείμενα), JetBrains Mono (`.num`).
- **Nav:** sticky κάψουλα με blur στο scroll. `main { position: relative }` στο global (για τα scatter logos).
  ΠΡΟΣΟΧΗ: το `main.page-in` αφαιρεί την κλάση του μετά το entrance animation (script στο Base) —
  αλλιώς «φυλακίζει» τα position:fixed παιδιά. ΜΗΝ το χαλάσεις.
- **Mobile-first:** ~80% επισκέπτες από κινητό. Κάθε αλλαγή ελέγχεται σε 375/390/430px.

## Header (αποφάσεις χρήστη)

- Μενού (29/07, απόφαση χρήστριας μετά από έρευνα NN/g): dropdown **«Υπηρεσίες»**
  (Google & ChatGPT · Αυτοματισμοί γραφείου · Κριτικές Google, με 1 γραμμή εξήγησης
  το καθένα) + «Τιμές · Αποτελέσματα · Οδηγοί». URLs αμετάβλητα. Mobile: χωρίς
  dropdown — τα 3 σκέτα links στο burger. Burger breakpoint 900px, κάψουλα scroll 1060px.
  Απορρίφθηκαν οι ετικέτες: «Ψηφιακή Προβολή/Αυτοματοποίηση» (αφηρημένες),
  «Site, Google & ChatGPT» (μακριά απαρίθμηση), γενικό «Προϊόντα».
- **Διάταξη header desktop (29/07, τελική μετά από 3 δοκιμές):** λογότυπο αριστερά,
  λινκ+ήχος+CTA = ΜΙΑ ενιαία παρέα κεντραρισμένη (16px κενά μεταξύ τους) — κορυφή
  1160px, κάψουλα scroll 960px. Δοκιμάστηκαν και ΑΠΟΡΡΙΦΘΗΚΑΝ: λινκ μόνα στο κέντρο
  με ήχο+CTA δεξιά («χώρια»), και full-width με ακριβές κέντρο + τέρμα δεξιά.
- Desktop: ΜΟΝΟ το πορτοκαλί CTA «Μέτρησε την επιχείρησή σου» → /tablo/ (+ εικονίδιο ήχου).
- Mobile: header = logo + burger ΜΟΝΟ. Μέσα στο μενού: links, διακόπτης «Ήχοι κλικ»,
  «Μέτρησε την επιχείρησή σου» (πορτοκαλί) + «Δωρεάν αξιολόγηση 45′» (ghost) — διακριτά.
- Η προώθηση τιμολόγησης 2026 ΒΓΗΚΕ τελείως (Ribbon δεν κάνει render, hero pill αφαιρέθηκε,
  homepage section παρκαρισμένο στο components/Einvoice2026.astro). Η σελίδα /timologisi-2026/
  ΔΕΝ υπάρχει πια (404) — παρκαρισμένη ως _timologisi-2026.astro, χωρίς footer link.

## Αρχική (index.astro)

- Δομή: Hero → Υπηρεσίες → Μεθοδολογία («Πώς δουλεύουμε — σε 4 βήματα») → Τιμολόγηση
  («Οι τιμές μας, δημόσιες — φτιάξτε το πακέτο σας») → Εταιρία → Τελικό CTA.
  Το section Αποτελεσμάτων ΑΦΑΙΡΕΘΗΚΕ από την αρχική (η σελίδα /apodeixeis/ υπάρχει).
- **Hero:** kicker «Ψηφιακή παρουσία & αυτοματοποίηση» (κεντραρισμένο με margin-inline:auto),
  τίτλος με χειροποίητη υπογράμμιση που ζωγραφίζει ο Ντοτ, κουμπιά:
  «Μέτρησε την επιχείρησή σου — 2′» (πορτοκαλί → /tablo/) + «Δωρεάν αξιολόγηση 45′» (ghost → /elegxos/).
- **Κάρτα κριτικών (hero):** λευκή κάρτα με G της Google, ετικέτα «Κριτικές», **4,5** αστέρια
  (πέμπτο μισογεμάτο, .stars-fill width:90%) και μετρητή που ανεβαίνει live έως **(2.000+)**.
  Εκκρεμεί απόφαση: μικρή εκδοχή και σε footer / σελίδα Τιμών.
- **Αιωρούμενα λογότυπα (11, αυθεντικά SVG στο `public/logos/`):** instagram (gradient από Wikimedia),
  facebook, google-gmail, google-icon, whatsapp, google-maps, tiktok (σε λευκό chip), linkedin,
  openai (class inv → invert για λευκό), viber + excel (από Wikimedia — η βιβλιοθήκη gilbarbara δεν τα έχει).
  - Desktop: όλα στο hero (θέσεις εγκεκριμένες — ΜΗΝ αλλάξουν), z-index 2 (πάνω από το mockup ταμπλό).
  - Mobile hero (θέσεις από κύκλους χρήστη): Instagram αριστερά τίτλου (19%), Google δεξιά τίτλου,
    ChatGPT/Excel στο τέλος της παραγράφου (40%).
  - Mobile υπόλοιπα 7: `.sflogo` ΑΓΚΥΡΩΜΕΝΑ ΣΤΑ SECTIONS (όχι % σελίδας — μετατοπιζόταν ανά συσκευή!):
    Gmail δίπλα «ΥΠΗΡΕΣΙΕΣ», WhatsApp τέλος Υπηρεσιών, Maps δίπλα «ΜΕΘΟΔΟΛΟΓΙΑ»,
    LinkedIn πάνω από «ΤΙΜΟΛΟΓΗΣΗ», Facebook δίπλα «ΤΙΜΟΛΟΓΗΣΗ», TikTok πριν «Εταιρία», Viber στο τελικό CTA.
  - Κινήσεις: λίκνισμα (translate/rotate στο img), parallax στο scroll (JS transform στο span, ΜΟΝΟ hero),
    είσοδος «απαλή ανάδυση με ξεθόλωμα» (flogoIn, κλιμακωτά nth-child στο hero, IntersectionObserver
    για τα sflogo). Όλα σέβονται prefers-reduced-motion.
  - Έλεγχος επικαλύψεων: τρέχει προγραμματιστικά (bounding boxes λογότυπα vs h1/h2/h3/p/btn/kicker/li)
    σε 375/390/430 — πρέπει να βγαίνει ΚΑΝΕΝΑ.
- Τελικό CTA: το φωτοστέφανο σβήνει ΜΕΣΑ στο section (radial at 50% 60%) — όχι κομμένη λωρίδα.

## Τιμές (/times/) — Package builder

- ΟΛΟΣ ο τιμοκατάλογος στο `src/data/pricing.ts` (κατηγορίες/επιλογές/πακέτα/FORM_ENDPOINT).
  Νέο automation = μία γραμμή. Το παλιό PRICES του config ΔΕΝ υπάρχει.
- Σειρά σελίδας: Hero → **Builder πρώτα** → Έτοιμα πακέτα → Φόρμα προσφοράς → Εγγύηση → FAQ → CTA.
- Πακέτα: Ξεκίνημα 260€, Ορατότητα 720€+215€/μ (highlighted), Πλήρες 850€+419€/μ —
  εκπτώσεις ΠΡΟΣΩΡΙΝΕΣ (TODO οριστικοποίηση). Social τιμές = χαμηλό άκρο ζώνης.
- Builder: ζωντανά σύνολα εφάπαξ/μηνιαίο, sticky δεξιά σε desktop, σταθερή μπάρα κάτω σε mobile.
  Preset click → φορτώνει επιλογές + δείχνει όφελος· χειροκίνητη αλλαγή → καθαρό άθροισμα.
- Φόρμα: ΕΝΕΡΓΗ μέσω FormSubmit (`FORM_ENDPOINT` στο pricing.ts, βλ. κορυφή αρχείου).
  Εφέ επιτυχίας «Δ» + auto-επαναφορά 6s. Έλεγχος πάντα του `json.success`, όχι μόνο res.ok.

## Ταμπλό (/tablo/) — Quiz

- 10 ερωτήσεις, μία ανά εργαλείο, σειρά: Προβολή (site, GBP, κριτικές, social, ChatGPT) →
  Αυτοματοποίηση (Excel, ταίριασμα πληρωμών, οφειλές, καθημερινή εικόνα, ανταγωνισμός).
- Αποτέλεσμα: 2 σκορ /100 (Ορατότητα, Αυτοματισμός) + ετυμηγορία «πόσο πίσω είσαι».
  Η εκτίμηση €/μήνα ΑΦΑΙΡΕΘΗΚΕ (απόφαση χρήστη).
- CTAs αποτελεσμάτων: «Διάλεξε το πακέτο σου →» (/times/) + «Δωρεάν αξιολόγηση 45′ μαζί μας» (/elegxos/).

## Ήχοι UI (Base.astro)

- Web Audio synth, διαφορετικός ήχος ανά ενέργεια (click/tick/nav/back/open/close/on/off),
  επέκταση με `data-sound="..."` σε οποιοδήποτε στοιχείο.
- Αυτοδιόρθωση iOS: προθέρμανση στο πρώτο άγγιγμα, αναδημιουργία κολλημένου AudioContext
  μέσα στο επόμενο tap, teardown σε bfcache restore. ΜΗΝ απλοποιηθεί.
- Διακόπτης: εικονίδιο στο nav (desktop) + σειρά «Ήχοι κλικ» στο mobile μενού, συγχρονισμένα.

## Mascot «Ντοτ»

- Particle τελεία με googly μάτια· γέννηση ανά σελίδα (map SPAWNS), ζωγραφίζει την υπογράμμιση
  του hero, στάσεις με data-mascot-stop. Κλικ = boing + ήχος.
- **Συννεφάκι ΑΦΑΙΡΕΘΗΚΕ οριστικά** — showBubble/hideBubble είναι no-ops. ΜΗΝ επανέλθει.
- **ΠΡΟΣΟΧΗ — Εφέ «μεταμορφώσεων/εκπομπής particles» του Ντοτ: ΔΟΚΙΜΑΣΤΗΚΑΝ ΚΑΙ
  ΑΦΑΙΡΕΘΗΚΑΝ ΟΡΙΣΤΙΚΑ (25/07/2026).** Δοκιμάστηκαν διαδοχικά: ολική διάλυση σε σχήματα,
  scroll-sync με αδράνεια/χρονόμετρα, εκπομπή 56 particles σε overlay — ο χρήστης τα απέρριψε
  όλα («δεν λειτουργεί καλά»). Το Mascot.astro επαναφέρθηκε στην καθαρή προ-εφέ κατάσταση
  (commit 68f5cd6) — ΜΗΝ ξαναπροταθούν εφέ που αλλάζουν τη συμπεριφορά του Ντοτ.
  Νέα κατεύθυνση — ΥΛΟΠΟΙΗΘΗΚΕ (25/07/2026): κινήσεις ΤΩΝ ΙΔΙΩΝ των sections στην αρχική,
  εγκεκριμένες μία-μία: Υπηρεσίες = υπογράμμιση τίτλου (.fx-uline/fxDraw), Μεθοδολογία =
  νούμερα βημάτων σκάνε (fxPop), Τιμολόγηση = αργός παλμός στο 2ο plan-mini (fxPulse 2.2s),
  Εταιρία = η γραμμή Εσείς—PISMA γεμίζει (.wb-fill/fxDraw + fxChip στο #bridge-pisma),
  Τελικό CTA = λάμψη στο .grad-text (fxSweep). Όλα στο index.astro: JS οπλίζει .fx-armed,
  IntersectionObserver βάζει .fx-on μία φορά (threshold .25)· χωρίς JS όλα ορατά,
  reduced-motion = καμία κίνηση. Desktop + mobile.

## Περιεχόμενο — ΚΡΙΣΙΜΟ

- **ΑΠΑΓΟΡΕΥΟΝΤΑΙ αναφορές σε «Γλυφάδα» και «Πάνο»** — αφαιρέθηκαν από παντού (τίτλοι, footer,
  schema, config). Ο έλεγχος: `grep -rn "Γλυφάδα\|Πάνος" src/` πρέπει να βγάζει τίποτα.
- Ύφος: επαγγελματικό, πληθυντικός στην αρχική (οι εσωτερικές μιλούν ακόμα ενικό — εκκρεμεί πέρασμα).
- Τιμιότητα: ΜΗΝ επινοηθούν πελάτες/μαρτυρίες. (Το «2.000+ κριτικές» μπήκε με ρητή απόφαση χρήστη.)
- **ΟΧΙ επινοημένα νούμερα/«σενάρια» πουθενά** (27/07 — αφαιρέθηκαν από τα Αποτελέσματα)·
  απλά λόγια χωρίς σλόγκαν· δίπλα στο «Google» και «AI/ChatGPT» όπου ταιριάζει.
- Email στο config: **info@pisma.gr (πραγματικό, ενεργό)**. Τηλέφωνο ακόμα placeholder (+30 210 000 0000).

## Ροή συνεργασίας με τον χρήστη (σημαντικό!)

- Γράφει ελληνικά χωρίς τόνους/με typos — θέλει σαφείς, απλές απαντήσεις στα ελληνικά.
- Για επιλογές design: θέλει ΠΡΩΤΑ προτάσεις/mockups στο chat (widgets) ή κλικαμπλ ερωτήσεις
  (AskUserQuestion) — ΟΧΙ απευθείας αλλαγές στον κώδικα. Μετά την έγκριση, υλοποίηση + build + push.
- Στέλνει screenshots (συχνά με κύκλους) — οι θέσεις του είναι νόμος· μετέφρασέ τες σε
  συντεταγμένες αγκυρωμένες σε στοιχεία (όχι % σελίδας).
- Απορρίπτει εύκολα μεγάλες αλλαγές — προτίμα μικρά αναστρέψιμα βήματα και κράτα restore points.

## ΕΚΚΡΕΜΟΤΗΤΕΣ

1. **Λόγια εσωτερικών σελίδων** — ΕΓΙΝΑΝ (23/07): se-vriskoun (hero «Ψηφιακή Προβολή»)
   και doulevei-mono-tou (hero «Αυτοματοποίηση», κάρτες = 6 αυτοματισμοί του builder).
   Κανόνας: κείμενα πληθυντικός, ΚΟΥΜΠΙΑ/CTA μένουν ενικός («Μέτρησε…»). ΕΚΚΡΕΜΟΥΝ
   ακόμα: tablo, elegxos, epikoinonia (πέρασμα ενικός→πληθυντικός στα κείμενα).
   Το apodeixeis ΞΑΝΑΓΡΑΦΤΗΚΕ 27/07 σε ενικό με ρητή έγκριση των κειμένων — ΜΗΝ «διορθωθεί».
2. **Footer labels** — ΕΓΙΝΕ (23/07): «Ψηφιακή Προβολή / Αυτοματοποίηση / Αποτελέσματα».
3. **Κάρτα κριτικών:** απόφαση 23/07 — μένει ΜΟΝΟ στο hero αρχικής προς το παρόν.
4. ~~FORM_ENDPOINT~~ ΕΓΙΝΕ (27/07, FormSubmit). ΕΚΚΡΕΜΟΥΝ: **οριστικές εκπτώσεις πακέτων** στο pricing.ts.
5. **Placeholder τηλεφώνου** (το email ΕΓΙΝΕ — info@pisma.gr).
5α. **Εργαλείο κριτικών — επόμενα:** εκτυπώσιμο QR-σταντάκι για τα demos, σελίδα
    υπηρεσίας στο site + τιμή στο pricing.ts (θέλει απόφαση χρήστριας), backend
    (μοναδικά κουπόνια, ρυθμίσεις ανά μαγαζί), επαναφορά soft check με πραγματικό link.
6. **Git υγιεινή:** warnings για corrupt objects στο `.git/gc.log` (τα push δουλεύουν) — μη επείγον.
7. **URL Αποτελεσμάτων:** παραμένει /apodeixeis/ ενώ η σελίδα λέγεται «Αποτελέσματα» —
   προτάθηκε /apotelesmata/ με redirect, δεν αποφασίστηκε ακόμα.
8. **Checklist Ελέγχου:** η σελίδα υπόσχεται καταγραφή «πριν» (τηλέφωνα, κριτικές, θέση σε
   Google & AI, ώρες γραφείου) — φτιάξε 10γραμμο checklist + υπενθύμιση επαναμέτρησης 3μήνου.

## Γνωστά τεχνικά quirks

- Browser pane: CSS transitions/rAF παγώνουν στο παρασκήνιο (ψευδο-αποτυχίες σε μετρήσεις)·
  screenshots μετά από scroll θέλουν το κόλπο `body.style.transform = translateY(...)` + `!important`
  (και ΔΕΝ πυροδοτεί IntersectionObserver — φυσιολογικό). Το Lenis μπλοκάρει προγραμματιστικό scroll.
- Vercel deploy: επιβεβαίωση με grep στο σωστό CSS chunk (συχνά `apodeixeis.*.css`, όχι `index.*.css`).
- Browser pane (27/07): σε αυτό το session τα rAF/frames πάγωναν τελείως (rafFired=false,
  document.timeline κολλημένο στο 0) → CSS animations/Lenis δεν «κουνιούνται», screenshots
  του /times μαύρα. Λύση: λειτουργικοί έλεγχοι με getComputedStyle + getBoundingClientRect
  (σύγκριση κέντρων), ΟΧΙ οπτικοί. Για δοκιμή φορμών χωρίς αληθινά emails:
  `window.fetch = async () => new Response(JSON.stringify({success:'true'}))` πριν το click.
- Τεστ στο live μέσα από το pane: cookie `document.cookie='pisma_code=pisma2026...'` + reload.
