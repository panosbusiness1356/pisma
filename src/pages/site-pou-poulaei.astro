---
import Base from '../layouts/Base.astro';
import PageHero from '../components/PageHero.astro';
import Faq from '../components/Faq.astro';
import CtaBand from '../components/CtaBand.astro';
import { SITE, ELEGXOS, eur } from '../config';
import { optionById } from '../data/pricing';

// Μία πηγή αλήθειας για τις τιμές: pricing.ts (web-basic/standard/pro).
const basic = optionById('web-basic')!;
const standard = optionById('web-standard')!;
const pro = optionById('web-pro')!;

const schema = {
  '@context': 'https://schema.org',
  '@type': 'Service',
  name: 'Site που πουλάει',
  serviceType: 'Κατασκευή ιστοσελίδων για επιχειρήσεις',
  areaServed: SITE.region,
  provider: {
    '@type': 'LocalBusiness',
    name: SITE.name,
    email: SITE.email,
    telephone: SITE.phone,
    url: SITE.url,
  },
  offers: {
    '@type': 'Offer',
    price: basic.price,
    priceCurrency: 'EUR',
    description: `Από ${eur(basic.price)} εφάπαξ.`,
  },
  description:
    'Γρήγορη, καθαρή ιστοσελίδα με ένα ξεκάθαρο επόμενο βήμα σε κάθε σελίδα — ο επισκέπτης δεν χαζεύει, σηκώνει το τηλέφωνο.',
};

const why = [
  {
    t: 'Γρήγορο — δεν φεύγει κανείς',
    d: 'Ανοίγει αμέσως, και στο πιο αργό κινητό. Κάθε δευτερόλεπτο καθυστέρησης είναι πελάτης που γύρισε πίσω στην Google.',
    icon: 'speed',
  },
  {
    t: 'Ένα ξεκάθαρο επόμενο βήμα',
    d: 'Κλήση, μήνυμα ή προσφορά — σε κάθε σελίδα, ο επισκέπτης ξέρει τι να πατήσει. Όχι λαβύρινθος, όχι «καλλιτεχνίες» που μπερδεύουν.',
    icon: 'target',
  },
  {
    t: 'Μετρήσιμο — ξέρετε τι απέδωσε',
    d: 'Πόσοι μπήκαν, πόσοι πάτησαν «Κάλεσε», από πού ήρθαν. Το site λογοδοτεί με νούμερα, κάθε μήνα.',
    icon: 'chart',
  },
];

const faqItems = [
  {
    q: 'Σε πόσο καιρό είναι έτοιμο;',
    a: 'Συνήθως σε 2–4 εβδομάδες από τη στιγμή που έχουμε τα υλικά (κείμενα, φωτογραφίες, λογότυπο). Αν λείπουν, βοηθάμε να φτιαχτούν.',
  },
  {
    q: 'Τι χρειάζεται να φέρω εγώ;',
    a: 'Ό,τι έχετε: λογότυπο, φωτογραφίες, τιμοκατάλογο, δυο λόγια για τη δουλειά σας. Τα υπόλοιπα — δομή, κείμενα, στήσιμο — τα αναλαμβάνουμε εμείς.',
  },
  {
    q: 'Το site θα είναι δικό μου;',
    a: 'Ναι, εξ ολοκλήρου. Domain, περιεχόμενο και αρχεία ανήκουν στην επιχείρησή σας — ό,τι κι αν γίνει με τη συνεργασία μας.',
  },
  {
    q: 'Πόσο κοστίζει;',
    a: `Τρεις καθαρές βαθμίδες, εφάπαξ: Basic ${eur(basic.price)}, Standard ${eur(standard.price)}, Pro ${eur(pro.price)}. Ό,τι μηνιαίο (π.χ. συντήρηση) είναι προαιρετικό και δημόσια τιμολογημένο στη σελίδα τιμών.`,
  },
  {
    q: 'Θα με βρίσκουν στη Google με αυτό;',
    a: 'Το site χτίζεται τεχνικά σωστό για τη Google από την πρώτη μέρα. Αν θέλετε και ενεργή άνοδο θέσεων (SEO), είναι ξεχωριστή υπηρεσία — τη βλέπετε στη σελίδα της Ψηφιακής Προβολής.',
  },
];
---

<Base
  title="Site που πουλάει — όχι βιτρίνα, εργαλείο δουλειάς | PISMA"
  description="Γρήγορο, καθαρό site με ένα ξεκάθαρο επόμενο βήμα σε κάθε σελίδα. Δείτε το ζωντανά: ο επισκέπτης μπαίνει, βλέπει, και το επόμενο βήμα του είναι το τηλέφωνό σας."
  schema={schema}
>
  <!-- 1. Hero -->
  <PageHero
    kicker="Site που πουλάει"
    sub="Γρήγορο, καθαρό, με ένα ξεκάθαρο επόμενο βήμα σε κάθε σελίδα. Ο επισκέπτης δεν χαζεύει — σηκώνει το τηλέφωνο."
  >
    Όχι βιτρίνα — <span class="accent">εργαλείο δουλειάς</span>
  </PageHero>

  <!-- 2. «Δείτε το ζωντανά» — σκηνή laptop σε loop όπως οι άλλες σκηνές:
       μέσα στην οθόνη ένα premium site οινοποιείου τριών «οθονών», και η κίνηση
       είναι απλώς η περιήγηση: αργό scroll προς τα κάτω με παύσεις, και
       επιστροφή στην κορυφή. Χωρίς JS ή με reduced motion: στατικό hero. -->
  <section class="section section--tight" aria-labelledby="sp-title">
    <div class="container">
      <div class="sp-grid">
        <div class="sp-copy">
          <div class="sec-head">
            <p class="kicker">Δείτε το ζωντανά</p>
            <h2 id="sp-title">Ο επισκέπτης μπαίνει — το τηλέφωνο χτυπάει</h2>
          </div>
          <p class="sp-sub">
            Έτσι δουλεύει ένα site-εργαλείο: μία δυνατή εικόνα, η λίστα με
            τις τιμές, και το τηλέφωνο εκεί που σταματά το μάτι. Το «επόμενο
            βήμα» του επισκέπτη είναι το ταμείο σας.
          </p>
          <div class="sp-cta-row">
            <a class="btn btn--orange" href="/times/">Δείτε τιμές για site →</a>
            <a class="btn btn--ghost" href="/site-demo/lyra/index.html" target="_blank" rel="noopener">Ανοίξτε το δείγμα ↗</a>
          </div>
          <p class="sp-note">Ζωντανό δείγμα δουλειάς μας — σκρολάρετε ελεύθερα μέσα στην οθόνη. Το δικό σας site στήνεται για τη δική σας δουλειά.</p>
        </div>

        <div class="sp-stage" id="sp-stage">
          <div class="sp-laptop">
            <div class="sp-screen">
              <span class="sp-camdot"></span>
              <div class="sp-page" id="sp-page">
                <div class="sp-browser">
                  <span class="sp-dots"><i></i><i></i><i></i></span>
                  <span class="sp-url">lyra-wines.gr</span>
                </div>
                <div class="sp-view" id="sp-view">
                  <iframe class="sp-iframe" id="sp-iframe" src="/site-demo/lyra/index.html" title="Ζωντανό δείγμα — Οινοποιείο Λύρα" loading="lazy" tabindex="-1" sandbox="allow-scripts allow-same-origin"></iframe>
                </div>
              </div>
            </div>
            <div class="sp-base"></div>
          </div>
        </div>
      </div>
    </div>
  </section>

  <!-- 3. Τι κερδίζεις -->
  <section class="section" aria-labelledby="why-title">
    <div class="container">
      <div class="sec-head">
        <p class="kicker kicker--blue">Τι κερδίζετε</p>
        <h2 id="why-title">Τρία πράγματα που ξεχωρίζουν το εργαλείο από τη βιτρίνα.</h2>
      </div>
      <div class="grid-3">
        {why.map((w) => (
          <article class="card reveal">
            <span class="icon-chip">
              {w.icon === 'speed' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <path d="M13 2.5 4.5 13.5h6l-1 8L19.5 10.5h-6z" />
                </svg>
              )}
              {w.icon === 'target' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <circle cx="12" cy="12" r="8.5" /><circle cx="12" cy="12" r="4.5" /><circle cx="12" cy="12" r="1" />
                </svg>
              )}
              {w.icon === 'chart' && (
                <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
                  <path d="M5 20V11" /><path d="M12 20V4" /><path d="M19 20v-6" />
                </svg>
              )}
            </span>
            <h3>{w.t}</h3>
            <p>{w.d}</p>
          </article>
        ))}
      </div>
    </div>
  </section>

  <!-- 4. Τιμή -->
  <section class="section section--tight" aria-labelledby="price-title">
    <div class="container container--narrow">
      <div class="card price-card reveal">
        <div class="price-main">
          <p class="kicker">Τιμές — δημόσιες, ίδιες για όλους</p>
          <h2 id="price-title">
            Από <span class="price-num num">{eur(basic.price)}</span> εφάπαξ
          </h2>
          <p class="price-sub">
            Τρεις καθαρές βαθμίδες: <strong>Basic {eur(basic.price)}</strong> ({basic.detail?.toLowerCase()}),
            <strong>Standard {eur(standard.price)}</strong> ({standard.detail?.toLowerCase()}),
            <strong>Pro {eur(pro.price)}</strong> ({pro.detail?.toLowerCase()}).
            Ό,τι μηνιαίο είναι προαιρετικό — και δημόσια τιμολογημένο.
          </p>
        </div>
        <div class="price-cta">
          <a class="btn btn--orange" href="/times/">Φτιάξτε το πακέτο σας →</a>
          <a class="btn btn--ghost" href="/elegxos/">Δωρεάν Έλεγχος {ELEGXOS.minutes}′</a>
          <span class="btn-note">Καμία πληρωμή online — το κλείνουμε στο τηλέφωνο.</span>
        </div>
      </div>
    </div>
  </section>

  <!-- 5. FAQ -->
  <section class="section" aria-labelledby="faq-title">
    <div class="container">
      <div class="sec-head">
        <p class="kicker">Συχνές ερωτήσεις</p>
        <h2 id="faq-title">Πριν ρωτήσετε, ρώτησαν άλλοι.</h2>
      </div>
      <Faq items={faqItems} withSchema />
    </div>
  </section>

  <!-- 6. CTA -->
  <CtaBand
    title="Ώρα το site σας να πιάσει δουλειά."
    sub={`Ξεκινάμε με τον δωρεάν Έλεγχο PISMA (${ELEGXOS.minutes} λεπτά) — βλέπουμε τι έχετε σήμερα και τι αφήνει στο τραπέζι.`}
    ctaText="Κλείστε τον δωρεάν Έλεγχο"
    ctaHref="/elegxos/"
    note="Καμία πληρωμή online · χωρίς δέσμευση"
  />
</Base>

<script>
  // «Site που πουλάει» (laptop): μέσα στην οθόνη ένα premium site τριών
  // οθονών, και η κίνηση είναι απλώς η περιήγηση — αργό scroll προς τα κάτω
  // με παύσεις σε κάθε ενότητα, και επιστροφή στην κορυφή. Χωρίς JS ή με
  // reduced motion: στατικό hero.
  // Μέσα στην οθόνη παίζει το πραγματικό δείγμα site (Οινοποιείο «Λύρα») σε
  // desktop απόδοση 1180px με σμίκρυνση — ο επισκέπτης σκρολάρει μέσα του
  // όπως σε κανονικό υπολογιστή.
  const spView = document.getElementById('sp-view');
  const spFrame = document.getElementById('sp-iframe') as HTMLIFrameElement | null;
  if (spView && spFrame) {
    const DESK = 1180;
    const fit = () => {
      const s = spView.clientWidth / DESK;
      spFrame.style.width = `${DESK}px`;
      spFrame.style.height = `${Math.round(spView.clientHeight / s)}px`;
      spFrame.style.transform = `scale(${s})`;
    };
    fit();
    if ('ResizeObserver' in window) new ResizeObserver(fit).observe(spView);
    else window.addEventListener('resize', fit);
  }
</script>

<style>
  /* ---------- «Site που πουλάει» — laptop ---------- */
  /* Δίστηλη διάταξη σε desktop (κείμενο αριστερά, laptop δεξιά), στοίβαγμα
     σε στενές οθόνες — ίδιο μοτίβο με τις άλλες σκηνές. */
  .sp-grid {
    display: grid;
    grid-template-columns: minmax(0, 1fr) auto;
    gap: clamp(28px, 5vw, 60px);
    align-items: center;
    max-width: 1020px; margin-inline: auto;
  }
  .sp-copy .sec-head { margin-bottom: 0; }
  .sp-sub { color: var(--muted); max-width: 44ch; margin: 16px 0 26px; }
  .sp-cta-row { display: flex; gap: 12px; flex-wrap: wrap; }
  .sp-note { color: var(--muted); font-size: .78rem; margin: 14px 0 0; max-width: 42ch; }
  @media (max-width: 960px) {
    .sp-grid { grid-template-columns: minmax(0, 1fr); }
    .sp-stage { margin-inline: auto; }
  }

  /* Ρεαλιστικό laptop: οθόνη 16:10 με λεπτό μπεζέλ και αλουμινένια βάση */
  .sp-stage { width: 520px; max-width: 100%; }
  .sp-laptop { position: relative; }
  .sp-screen {
    position: relative;
    background: #0f0f11; border: 1px solid #3c3c42;
    border-radius: 16px 16px 4px 4px;
    padding: 11px 11px 15px;
    box-shadow: 0 24px 70px rgba(0, 0, 0, .55);
  }
  .sp-camdot {
    position: absolute; top: 5px; left: 50%; transform: translateX(-50%);
    width: 4px; height: 4px; border-radius: 50%;
    background: #2c2c31; box-shadow: inset 0 0 2px #000, 0 0 0 1px rgba(255, 255, 255, .05);
  }
  .sp-base {
    width: 118%; margin-left: -9%;
    height: 14px;
    background: linear-gradient(180deg, #eceef1 0%, #c3c5cb 50%, #9a9ca3 100%);
    border-radius: 2px 2px 14px 14px;
    position: relative;
  }
  .sp-base::before {
    content: ''; position: absolute; top: 0; left: 50%; transform: translateX(-50%);
    width: 84px; height: 5px;
    background: #a2a4ab; border-radius: 0 0 9px 9px;
    box-shadow: inset 0 1px 2px rgba(0, 0, 0, .18);
  }
  .sp-base::after {
    content: ''; position: absolute; top: 0; left: 2%; right: 2%;
    height: 1px; background: rgba(0, 0, 0, .18);
  }

  /* Η σελίδα μέσα στην οθόνη — φωτεινό editorial site κτήματος: ζεστή
     άμμος, βαθύ λαδί και τερακότα (συγγενική με τα ιταλικά estates παλέτα,
     όχι αντιγραφή), μεγάλα serif κεφαλαία με αραίωση. */
  .sp-page {
    position: relative;
    display: flex; flex-direction: column;
    aspect-ratio: 16 / 10;
    background: #150C11;
    border-radius: 6px; overflow: hidden;
  }
  .sp-view { position: relative; flex: 1; overflow: hidden; }
  /* Το πραγματικό δείγμα-site μέσα στην οθόνη: desktop απόδοση σε σμίκρυνση */
  .sp-iframe {
    border: 0; display: block;
    transform-origin: 0 0;
    background: #150C11;
  }
  .sp-browser {
    display: flex; align-items: center; gap: 8px;
    background: #1d1c21; padding: 6px 10px;
    border-bottom: 1px solid rgba(255, 255, 255, .05);
  }
  .sp-dots { display: inline-flex; gap: 5px; }
  .sp-dots i { width: 8px; height: 8px; border-radius: 50%; }
  .sp-dots i:nth-child(1) { background: #ff5f57; }
  .sp-dots i:nth-child(2) { background: #febc2e; }
  .sp-dots i:nth-child(3) { background: #28c840; }
  .sp-url {
    flex: 1; text-align: center;
    background: rgba(255, 255, 255, .08); border-radius: 999px; padding: 3px 10px;
    color: #aeb6c2; font-size: .64rem; font-weight: 600;
  }

  @media (max-width: 560px) {
    /* Η προεξοχή της βάσης μαζεύει ώστε να μη δημιουργεί οριζόντιο scroll */
    .sp-base { width: 106%; margin-left: -3%; }
  }

  /* ---- Τιμή (ίδιο μοτίβο με τις άλλες σελίδες υπηρεσιών) ---- */
  .price-card {
    display: grid;
    grid-template-columns: 1.2fr minmax(min(240px, 100%), .8fr);
    gap: clamp(24px, 4vw, 48px);
    align-items: center;
    padding: clamp(26px, 4vw, 48px);
  }
  @media (max-width: 720px) {
    .price-card { grid-template-columns: 1fr; }
  }
  .price-main .kicker { margin-bottom: 14px; }
  .price-num { font-size: clamp(2rem, 5vw, 2.8rem); color: var(--orange-text); }
  .price-sub { margin-top: 12px; color: var(--muted); }
  .price-sub strong { color: var(--ink); }
  .price-cta { display: flex; flex-direction: column; gap: 12px; }
  .price-cta .btn { text-align: center; }
  .price-cta .btn-note { text-align: center; }
</style>
